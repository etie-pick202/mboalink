# Ne PAS définir ici org.gradle.java.home : un chemin absolu (ex. un JDK
# Windows « C:\Program Files\... ») casse le build sur les autres postes
# et sur le CI Linux (« Java home supplied is invalid »).
#
# La version de Java est déjà imposée par le toolchain dans build.gradle
# (Java 17) — Gradle la détecte/provisionne automatiquement.
#
# Pour un override local propre à ta machine, utilise plutôt
# ~/.gradle/gradle.properties (hors dépôt), jamais ce fichier versionné.
